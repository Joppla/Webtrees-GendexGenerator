<?php
use Fisharebest\Webtrees\I18N;
?>

<h1><?= $this->title() ?></h1>

<form method="POST">
    <div class="card">
        <div class="card-body">

            <!-- Enable/Disable for specific trees -->
            <fieldset class="form-group">
                <legend><?= I18N::translate('Select family trees to include in GENDEX') ?></legend>
                <p class="text-muted small"><?= I18N::translate('Choose which family trees should be included when generating the GENDEX file.') ?></p>
                
                <?php foreach ($trees as $tree): ?>
                    <div class="form-check">
                        <input 
                            class="form-check-input" 
                            type="checkbox" 
                            name="enabled_trees[]" 
                            value="<?= $tree->id() ?>"
                            id="tree_<?= $tree->id() ?>"
                            <?= in_array($tree->id(), $enabled_trees) ? 'checked' : '' ?>
                        >
                        <label class="form-check-label" for="tree_<?= $tree->id() ?>">
                            <?= $tree->title() ?>
                        </label>
                    </div>
                <?php endforeach; ?>
            </fieldset>

            <hr>

            <!-- Date format option -->
            <fieldset class="form-group">
                <div class="form-check">
                    <input 
                        class="form-check-input" 
                        type="checkbox" 
                        name="year_only" 
                        id="year_only"
                        <?= $year_only ? 'checked' : '' ?>
                    >
                    <label class="form-check-label" for="year_only">
                        <?= I18N::translate('Use year only for dates (YYYY format)') ?>
                    </label>
                </div>
            </fieldset>

            <hr>

            <!-- Include birthplace -->
            <fieldset class="form-group">
                <div class="form-check">
                    <input 
                        class="form-check-input" 
                        type="checkbox" 
                        name="include_birthplace" 
                        id="include_birthplace"
                        <?= $include_birthplace ? 'checked' : '' ?>
                    >
                    <label class="form-check-label" for="include_birthplace">
                        <?= I18N::translate('Include birth place in GENDEX') ?>
                    </label>
                </div>
            </fieldset>

            <hr>

            <!-- Include death information -->
            <fieldset class="form-group">
                <div class="form-check">
                    <input 
                        class="form-check-input" 
                        type="checkbox" 
                        name="include_death" 
                        id="include_death"
                        <?= $include_death ? 'checked' : '' ?>
                    >
                    <label class="form-check-label" for="include_death">
                        <?= I18N::translate('Include death information in GENDEX') ?>
                    </label>
                </div>
            </fieldset>

        </div>

        <div class="card-footer">
            <button type="submit" class="btn btn-primary">
                <?= I18N::translate('Save') ?>
            </button>
        </div>
    </div>
</form>